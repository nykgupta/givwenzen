### Sometimes the defaults are not good enough ###

Where as we tried to follow the idea of convention over configuration we also give the ability to change the defaults.

Defaults:

  * StepClassLocation
  * CallingOtherSteps
  * CustomStateForSteps
  * StepClassInstantiation